1. Стоит ли писать отдельный сервис для управления загрузкой данных в
localStorage?
2. Когда применять type, interface, class, dto для описания типов данных?
3. Когда лучше применять set и get в классах и объектах?
4. Стоит ли сделать сначала Class AuthData (в котором описать get и set) и от
него уже наследовать Auth?
5. Что такое observable pipe и прочее на примере login (нет данных, хотя в http
ответ есть)
